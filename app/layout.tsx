import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Scalable Portfolio Converter',
  description:
    'Convert Scalable Capital transaction CSV exports to TradingView and Wealthfolio portfolio formats',
  keywords: [
    'Scalable Capital',
    'TradingView',
    'Wealthfolio',
    'portfolio',
    'CSV',
    'converter',
    'investment',
    'trading',
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
